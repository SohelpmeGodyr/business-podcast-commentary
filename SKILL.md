---
name: business-podcast-commentary
description: 将商业访谈、播客逐字稿重构为有证据边界的深度商业评述，对承重事实进行交叉验证。专注于总结概括、事实核查与隐性信号推演三大能力。适用于分析商业播客、创始人访谈、投资人对谈、财报电话会和长篇行业讨论；当用户要求创建或更新飞书云文档时，结合 lark-doc 交付为高可读性富文档。
---

# 商业播客深度评述

将一场对话还原为读者可以检视的商业论证，而不是把逐字稿压缩成主题清单。

## 先界定分析范围

先识别或明确说明以下默认项：

- **读者与决策问题**：投资人、经营者、创作者、平台团队或泛商业读者；说明读者希望据此做什么判断。
- **研究模式**：始终执行事实核查。对承重事实进行交叉验证——将说话者的外部事实主张与公开独立来源比对，在正文中呈现原话与核查结果的对照。核查协议见 [references/fact-check-protocol.md](references/fact-check-protocol.md)。
- **截止时间与交付形式**：文章、管理层简报，或文章加证据台账。

若逐字稿无法支持一个连贯的中心论点，呈现两种或多种竞争性解释。不要为了套用模板而强行制造统一结论。

## 使用编辑工作流

### 1. 审计原始材料

识别说话者、其业务角色、知识边界和可能的商业利益。区分一线经营观察、投资判断、平台口径与主持人的挑衅性提问。

建立简短的待核术语表，收录可能由语音识别造成的不确定公司名、产品名、指标和缩写。未核验前保留不确定性，不静默改写为确定名称。

**比较句方向校验**：遇到"A 的 X 相当于 B 的 Y"类比较句时，先翻译成大白话确认方向——"谁的贵、谁的便宜，谁的多、谁的少"。确认无误后再往下提取。禁止将比较方向写反。

### 2. 提取并交叉验证事实主张

**必须执行网络搜索，不得跳过。** 禁止在文末写"建议读者自行核查"而不实际搜索，禁止将所有主张标注为"未经核验"而不尝试验证，禁止凭 agent 自身知识判断数据真伪。每条承重主张至少执行一次搜索。

协议见 [references/fact-check-protocol.md](references/fact-check-protocol.md)。

**提取与分级**：从逐字稿中逐条提取说话者引用的外部事实主张。每条保留原话，不概括、不润色。同时判定承重度——`承重`（错了会推翻核心结论）、`支撑`（强化论点但不改变结论方向）、`背景`（删掉不影响判断）。说话者对自己企业当前做法的描述和主观判断不进入核查范围。**唯一例外**：当某条自述作为隐性信号推演的承重依据时（见第 6 节"隐性信号推演"），该条自述升级进入核查范围，按承重主张同等处理。

**分批核查**：只核查承重级主张。按优先级每批最多 10 条执行核查——判断来源独立性、搜索独立来源、标记核查标签。每批完成后将核查结果直接写入文档正文——已确认的带 `✅` + 来源，无法验证的带 `⚠️ 未经核验` + 说明，存疑的带 `❌ 存疑` + 反证。支撑级和背景级在正文中标注"据 XX 称"，不进入核查批次。

**暂停与安全网**：所有承重主张核查完毕或用户确认停止后，剩余的 `⚠️` 标签统一代表"无法独立验证"而非"还没查"。用户要求停止但还有承重主张未核查时，必须提示并二次确认。

核查结果台账将作为步骤 3（主张台账）和步骤 7（证据台账）的输入。

### 3. 写作前建立主张台账

只提取承重主张。数量由内容决定，不设上限。筛选标准：假设这条主张不成立，评述的核心结论会垮吗？会垮才算承重，不垮就不放进来。

- 主张内容与提出者；
- 时间戳或来源位置；
- 证据类型与可信度；
- 因果机制：`原因 → 机制 → 结果`；
- 前提条件与受影响的利益相关方；
- 最强反方解释或边界条件；
- 能削弱该主张的证据或领先指标。

需要建立台账、辨析数据口径或判断可信度时，阅读 [references/evidence-ledger.md](references/evidence-ledger.md)。

**优先级判断**：扫逐字稿时，以下内容天然权重更高，优先提取：

- 数字和具体数据 ＞ 形容词和模糊描述（"营收 50 亿"比"做得不错"重要）
- 矛盾和冲突 ＞ 共识和附和（两个说话者看法相反的地方，往往是核心问题所在）
- 具体案例和亲身经历 ＞ 泛泛而谈和行业常识（"我们试了 X，结果是 Y"比"现在大家都在做 X"重要）
- 说话者反复强调或明确说"关键""核心""最重要的" ＞ 一笔带过的信息
- 说话者自己的经营实操 ＞ 他对别人/别的公司的推测

**覆盖度检查**：提取完主张后，检查是否遗漏：

- 每个说话时长占比超过 10% 的嘉宾，至少有一个主张体现在台账中——不只是主持人的框架，嘉宾的核心判断也必须覆盖
- 被讨论了超过 5 分钟的话题，至少有一个对应的主张——讨论时长本身就是重要性的信号
- 如果两个以上的说话者就同一个问题发生了争论或反复讨论，这个问题大概率是一个承重主张

统一使用以下证据标签：

| 标签 | 使用情形 |
|---|---|
| `直接陈述` | 来源中可定位、可标注时间戳的原话。 |
| `嘉宾估算` | 嘉宾给出的经营数据、估算或解释。 |
| `公开核验` | 已核对的一手公开材料，并在正文中给出出处。 |
| `交叉验证` | 经网络搜索找到独立公开来源比对后的结果。标注核查标签（已确认/部分确认/无法验证/存疑）和独立来源数量。 |
| `编辑推演` | 基于已陈述证据形成的结论；说明推演过程。 |
| `待验证` | 缺乏充分支撑的关键断言；说明仍缺少什么证据。 |

### 4. 建模商业系统

按经营链路建模，不按发言顺序复述：

`投入要素 → 内容生产 → 分发 → 触达/获客 → 变现 → 结算 → 反馈回路`。

对每一环说明决策者、稀缺资源、成本承担方、控制点和可衡量产出；再判断变化是在创造新增价值、重新分配存量价值，还是两者并存。

将”成本下降””进入门槛下降””利润扩大”和”价值被谁捕获”视为不同主张。它们常由不同主体实现，也常出现相反结果。

**找瓶颈，不只填信息**：每写完一个环节，追问”这个环节里，什么资源最缺？什么成本最高？什么环节最卡脖子？”那个最卡脖子的东西就是这个环节的约束。写总结时以约束为主线——产能过剩但分发成本卡脖子，那就围绕”分发卡脖子”展开，而不是把”产能高”和”分发贵”各写一段相安无事。不是”他说了什么”按类别排列，而是”这盘生意真正卡在哪”作为主线。

### 5. 核查证据与财务语言

对每个重要数字记录指标定义、单位、地域、时间范围、分子、分母和来源。清楚区分广告消耗、GMV、收入、毛利、净利、平台抽成和创作者分成。

优先核对会改变结论的承重事实；优先使用财报、公司公告、监管文件、原始数据集和一手访谈。

### 6. 撰写面向读者的商业评述

文档标题直接使用源逐字稿的文件名，不另起标题。

**信息密度是最高原则。** 正文中每一个论点必须有具体的数字、案例或机制支撑。不写"花了很多钱"，写"年度预算接近 1000 亿"。不写"增长很快"，写"半年 DAU 从几百万反超到领先"。禁止泛泛而谈。

**禁止无参照判断。** 任何评价性表述（"这个数字很高""这个比例合理""这个增速不算极端"）必须有对比基准——要么来自说话者自己的比较，要么来自独立搜索的行业数据。没有基准的判断不许出现在正文中。如果搜不到基准，只能写"该数据缺乏行业对比基准"。禁止 agent 凭自身知识做出"这个比例看起来不极端"之类的判断。

**说话者原话与核查结论必须泾渭分明。** 正文中引用说话者的数据主张时，先完整写出说话者的原话（黑色正文）。然后**另起一行，单独一段**，用带颜色的文字写出核查结论。核查结论禁止和说话者原话放在同一段内——读者一眼扫过去必须能分清哪段是"他说的"、哪段是"agent 查的"。

**核查只针对说话者原话，不对 agent 自己的分析做核查。** 彩色核查结论只能出现在说话者逐字稿原话之后。agent 自己的分析、推演、总结不允许自带核查标签——agent 不能自己造一个判断然后自己"验证"它。如果 agent 需要在正文中做出分析性判断（如"该场景正在衰退""该趋势不可持续"），必须明确标注"编辑分析"字样，且不得附加核查颜色。示例：

> 正确：据张某在访谈中称，其所在业务的年度预算接近某一规模。
>
> <span text-color="orange">🟡 经核查，该公司未公开披露该业务的独立预算数据，因此无法通过公开来源确认，仅能作为张某的个人估算。</span>
>
> 错误：某消费场景正在消失。<span text-color="green">🟢 经核查，公开数据呈下降趋势。</span>    ← “某消费场景正在消失”不是逐字稿原话，是 Agent 自己生成的判断；即使颜色语法正确，也不能给编辑判断附核查标签

核查结论使用飞书 XML 彩色文字语法 `<span text-color="颜色">`：已确认 = `green`、无法验证 = `orange`、存疑 = `red`。语法示例：`<span text-color="green">🟢 经核查，...</span>`。emoji 是辅助，**文字颜色是主体**。只加 emoji 不用 `<span text-color>` 属于格式错误。

**开篇元数据卡片**（紧跟标题之后）：

- 播客名、主持人、嘉宾（姓名 + 当时的角色）、时长、日期

**开篇摘要**（元数据卡片之后，不超过 200 字）：这期聊了什么 + 核心判断是什么 + 最关键的一个数字。

**关键数据速览**（摘要之后、正文之前）：从逐字稿中提取说话者明确提到的所有数字，汇总为表格。列：指标 | 数据（逐字稿原话） | 核查结果（颜色标签）。**表格只收录逐字稿里说话者说过的数字，禁止放入 agent 从网上搜到的、逐字稿里没提的数据。** 每一个数字必须能定位到逐字稿时间戳。

核查结果用颜色标签标注：`🟢 已确认`（2+ 独立来源验证）、`🟡 部分确认`（1 个来源或有口径差异）、`🟡 无法验证`（搜了但没找到独立来源）、`🔴 存疑`（独立来源与该数据矛盾）。核查标签让读者一眼看清哪些数字可信、哪些只有一面之词。

**正文结构**（关键数据速览之后）：按内容的自然逻辑组织，不强制套用固定模板。对话是主题式的就按主题切，是产品案例式的就按产品切，是时间线的就按时间线切，几种方式也可以混用。唯一的要求：**把逐字稿里的重要内容尽可能保留下来，不因为套模板而丢弃信息。** 如果吃不准该用哪种结构，以下七段为默认选项：

1. 这次聊了什么、能信几分
2. 发生了什么变化、为什么是现在
3. 这门生意是怎么转起来的
4. 谁是赢家、谁被抢了蛋糕
5. 什么地方可能翻车
6. 接下来可能怎么走
7. **值得监控的关键信号指标**：如果你是投资人/从业者/同行，接下来该关注什么信号、什么指标。

**隐性信号推演**（仅承重叙事，正文写作时执行）：承接第 3 节的承重分级与第 4 节的系统建模，对承重叙事执行隐性信号挖掘——从说话者话语中反推表面引导之外的隐性顾虑与深层指向。完整方法论、六维检测框架与案例见 [references/implicit-signals.md](references/implicit-signals.md)。执行级规则如下：

- **触发条件**（满足任一才挖掘，其余叙事不做隐性信号分析）：① 行为/选择服务于某条承重主张；② 言行差——表态与实际行为不一致处；③ 高成本/不可逆选择（自建、长约、全量铺开）；④ 说话者刻意强调或回避的自身动作。
- **三档分级**：
  - `可判定`：六维检测到异常 + 至少两维独立指向同一方向 + 关键结构特征已核查为真。输出完整条目：显性引导 A / 隐性顾虑 B / 深层指向 C + 推演论据 + 反方解释 + 可证伪条件。A/B/C 不齐全时如实写，不硬凑。
  - `存在但不可判定`：六维检测到异常，但无法多信号印证、或各维指向矛盾、或关键背景缺失。输出压缩条目：检测到的异常特征 + 并列候选解读（不选边）+ "要判定缺什么信息"。必须明确写出"现有信息不足以判定"。
  - `无信号`：不写、不标注。
- **插入位置**：承重叙事的正文段与核查段之后，紧跟一个隐性信号 callout。
- **与第 7 段的协调**：callout 内的可证伪条件/待观察项必须汇入文末第 7 段监控清单，callout 内一句带过（"检验条件见监控清单 #N"），禁止两处重复详述。
- **硬性纪律**：① 全部标注"编辑推演"，callout 呈现（`可判定`=黄色、`存在但不可判定`=灰色），callout 内**禁止**出现 `<span text-color>` 核查颜色——推演层与核查层严格分离；② 每条必有反方解释与可证伪条件，缺一不可；③ 异常判定必须写明基线（相对该说话者通篇表达模式，或知识库中的历史模式）；④ 推演增强统一称"多信号印证"，禁止用"交叉验证"指代（该词专指事实核查标签）；⑤ 推演所依据的说话者自述升级进入核查范围（见第 2 节例外）；⑥ 语料边界：仅限本期逐字稿 + 公开网络信息 + 知识库 speaker 档案，禁止凭 agent 自身印象推断。

启用了研究核验模式时，在正文中以轻量标签标注已核查主张的状态，并在文章末尾附独立的"事实核查"板块——逐条呈现原话引用、核查发现、独立来源和结论。说话者原话与核查结果的对照是读者判断信息可信度的关键界面，不得省略。

逐字稿原文置于文档最下方，必须展开呈现、全文可见，不得折叠收起。标题为"📄 原始逐字稿"。附录逐字稿是为了让读者可以自行核对原话出处，不替代正文分析。

### 7. 在需要深度时附上证据台账

当用户要求严谨分析、事实核验、投资视角或可复用研究资料时，在文章后附简明证据台账。使用参考文件中的模板，并使台账与面向读者的叙事分离。

### 8. 完稿前运行质量检查

逐项确认：

- 每个承重结论都能追溯到证据，或明确标为编辑推演；
- 说话者利益位置和事实状态在关键处可见；
- 财务指标没有跨口径直接比较；
- 最强竞争性解释获得公平呈现；
- 未来判断是附有可观察信号的条件化情景，而不是确定性预测；
- 结论说明了尚未知道什么，以及什么新证据会改变判断；
- 没有用脱离语境的”金句墙”或泛泛的”新人建议”替代分析；
- 目标为飞书文档时，层级、颜色和结构化组件都服务于读者的判断，不造成额外阅读负担；
- 研究核验模式下：每条事实主张都有核查结果和搜索记录；未找到独立来源的条目已显性标注”无法验证”；已确认条目附可证伪条件；来源独立性已检查（转载不算独立来源）；
- 事实核查板块中，原话与逐字稿逐字一致，核查结论不超出搜索来源实际支持的范围；
- 正文中所有核查结论均使用 `<span text-color=”...”>` 改变了文字颜色。仅加 emoji 不用 span 视为格式错误；
- 隐性信号条目全部标注”编辑推演”，callout 内无 `<span text-color>` 核查颜色混入；
- 隐性信号三档分级无放水：`可判定`条目能在逐字稿中定位到两处以上独立语言特征，且所依据的结构特征已核查为真；`可判定`条数不高于`存在但不可判定`条数，超出即为质量警报；
- 每条隐性信号均有反方解释与可证伪条件，异常判定已写明基线；
- 隐性信号的可证伪条件已汇入第 7 段监控清单，无两处重复详述。

### 9. 交付为飞书高可读性富文档

**飞书富文档是本 Skill 的默认输出方式。** 评述完成后必须调用 `lark-doc` 交付为飞书文档，不得以纯文本交付。将本 Skill 视为内容与分析标准，将 `lark-doc` 视为飞书文档操作和 XML 格式标准。

**直接写入源文档（默认）**：当逐字稿本身是飞书文档时，评述内容直接写入该源文档——将正文插入到源文档的逐字稿之前，不新建文档；文档标题沿用源文档标题（即源逐字稿文件名），不另起标题。逐字稿保持在全文最下方，必须展开、不得折叠。仅当逐字稿不是飞书文档（聊天文本、本地文件等）时，才新建飞书文档，并将逐字稿全文置于文档最下方、展开呈现。

执行飞书文档写入前，先按 `lark-doc` 的要求阅读认证、XML、样式以及创建或更新工作流；创建富文档时优先使用 XML。先完成内容判断与证据审计，再设计版式；不要以装饰替代分析。

阅读 [references/lark-doc-layout.md](references/lark-doc-layout.md)，并按以下原则交付：

- 使用 `<title>` 与 `h1`—`h3` 建立阅读层级。文档标题（`<title>` 和 `h1`）直接使用源逐字稿的文件名，不另起标题。标题之后紧跟元数据卡片：播客名、主持人、嘉宾（姓名 + 角色）、时长、日期，使用 `<callout>` 蓝色呈现，不占正文篇幅。
- 关键数据速览使用 `<table>` 呈现，列：指标、数据（逐字稿原话）、核查结果（🟢/🟡/🔴 颜色标签），置于摘要之后、正文之前。每行数据的核查标签必须在事实核查板块中有对应的完整记录。
- 将”编辑判断””关键风险””待验证项”分别映射为少量语义明确的 `<callout>`；使用蓝色表示说明、绿色表示已验证或推荐、黄色表示待确认、红色表示风险。颜色必须配合文字和 emoji，不只依赖颜色。
- 隐性信号使用独立 `<callout>`：`可判定`=黄色、`存在但不可判定`=灰色，标题以 💧 开头并标注”编辑推演”。callout 内**不得**使用核查颜色 `<span text-color>`——推演层与核查层视觉严格分离。
- 事实核查板块使用 `<callout>` 逐条呈现，颜色按核查结果：绿色（已确认）、黄色（部分确认 / 无法验证）、红色（存疑）。每条包含原话 `<quote>` 引用、核查发现和独立来源链接。板块以 `<h2>🔎 事实核查</h2>` 为标题，置于正文之后、证据台账之前。
- **正文中核查结论必须用 `<span text-color=”色名”>` 改变文字颜色**：已确认 = `green`、无法验证 = `orange`、存疑 = `red`。飞书 XML 不支持 CSS hex 色码，只支持 7 种命名色。emoji 是辅助，颜色是主体。示例：`<span text-color=”orange”>🟡 经核查，该数据无法通过公开来源确认。</span>`
- 用 `<grid>` 组织两个简短、需要并读的卡片式信息块，例如”机会 / 约束”或”支持证据 / 反方证据”。用 `<table>` 承载多维比较、数据口径和情景矩阵；用 `<checkbox>` 承载后续观察信号或决策清单。
- 当价值链、因果链、平台机制、飞轮或情景路径是文章的核心论据时，使用 `lark-doc` 规定的画板方式插入 Mermaid 或 SVG 图示。图示必须降低理解成本，而非重复正文。
- **自动检测图表需求**：逐字稿中出现以下语言特征时，说明说话者在口头描述一个图表或数据可视化，必须自动生成对应的图表——“X轴””Y轴””坐标系””曲线””走势””拐点””峰值””谷底””从X涨到Y再跌到Z”等按时间序列变化的数据对比。使用飞书画板或 Mermaid 呈现，插入到对应的正文段落旁边。
- 将来源口径、研究范围和待验证事项显性写入文档；保留本 Skill 的证据标签与财务口径约束。
- 编辑已有文档时，先获取对应 block ID、沿用既有结构与风格，再按 `lark-doc` 的局部更新规则写入。不要用整段覆盖破坏已有素材。
- 将”卡片式排版”实现为 `<callout>`、`<grid>`、表格或链接预览；仅在拥有已有聊天卡片的 `chat-id` 时使用 `<chat_card>`，不要将其当作通用视觉卡片。
- 逐字稿原文置于文档最末（证据台账之后），必须展开、全文可见，不得使用默认收起的折叠块。标题为”📄 原始逐字稿”。

## 默认交付形态

除非用户另有要求，输出一篇 1,800—20,000 字的中文商业评述，后附简明证据台账。议题较窄时压缩章节；价值链复杂时增加机制解释的篇幅。
